spring:
  application:
    name: service-sms
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 #nacos中心地址
        file-extension: yml # 配置文件格式，如果是properties文件则不用配置此项
